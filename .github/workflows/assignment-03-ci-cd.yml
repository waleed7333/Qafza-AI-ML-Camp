name: Assignment 03 CI/CD

on:
  push:
    branches:
      - main
      - assignment-03
    paths:
      - "assignments/03-olist-late-delivery-mlops/**"
      - ".github/workflows/assignment-03-ci-cd.yml"
  pull_request:
    paths:
      - "assignments/03-olist-late-delivery-mlops/**"
      - ".github/workflows/assignment-03-ci-cd.yml"

permissions:
  contents: read
  packages: write

jobs:
  quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: assignments/03-olist-late-delivery-mlops

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            assignments/03-olist-late-delivery-mlops/requirements/runtime.txt
            assignments/03-olist-late-delivery-mlops/requirements/dev.txt

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements/runtime.txt -r requirements/dev.txt
          python -m pip check

      - name: Lint
        run: python -m ruff check app src scripts tests

      - name: Check formatting
        run: python -m ruff format --check app src scripts tests

      - name: Run tests
        run: python -m pytest

  docker:
    runs-on: ubuntu-latest
    needs: quality

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Validate Docker Compose
        working-directory: assignments/03-olist-late-delivery-mlops
        run: docker compose config > /dev/null

      - name: Verify external service images are pullable
        run: |
          docker pull postgres:16@sha256:4b7183ac05f8ef417db21fd72d71047a4238340c261d3cc3ddb6d579ab5071ae
          docker pull quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z
          docker pull quay.io/minio/mc:RELEASE.2025-08-13T08-35-41Z
          docker pull prom/prometheus:v2.55.1

      - name: Build bootstrap development image
        working-directory: assignments/03-olist-late-delivery-mlops
        run: docker build -f Dockerfile.dev -t qafza-assignment-03-dev:ci .

      - name: Log in to GHCR
        if: github.event_name == 'push'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and optionally push API image
        uses: docker/build-push-action@v7
        with:
          context: assignments/03-olist-late-delivery-mlops
          file: assignments/03-olist-late-delivery-mlops/Dockerfile
          push: ${{ github.event_name == 'push' }}
          tags: ghcr.io/waleed7333/qafza-assignment-03:${{ github.sha }}
